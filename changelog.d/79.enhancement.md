Add evaluator provenance attributes (`evaluator.id`, `evaluator.version`, `evaluator.type`, `reference_set.id`, and `scope`) to the `gen_ai.evaluation.result` event. `evaluator.type` includes an `outcome` value for scores derived from an observed real-world result rather than from a check, a model's opinion, or a reviewer's judgment.
